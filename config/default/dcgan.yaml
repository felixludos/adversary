
parents: [base]

latent_dim: 100

training:
  epochs: 200

dataset.batch_size: 64

model:
  _model_type: dcgan

  generator._model_type: dcgan-gen
  discriminator._model_type: dcgan-disc

  optim_type: adam
  lr: 0.0002
  beta1: 0.5
  beta2: 0.999







